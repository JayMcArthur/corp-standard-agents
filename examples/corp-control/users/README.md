# User Profiles

Each developer gets a corp-resident profile under `users/<username>/`.

This example repo includes `users/alice/` to model the normal person-onboarding path:

- `team-agents setup --corp-repo /path/to/corp-control --user alice`
- `team-agents attach`
