# Example Workspaces

These folders are created by `scripts/bootstrap_examples.sh`.

They are intentionally not checked in as git repos because the product identifies real repos by local `.git` state and remotes.

