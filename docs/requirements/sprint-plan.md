# Archived Sprint Plan

This plan is obsolete.

It sequences the older Codex-first requirements set and should not be used for current execution.

Current sequencing lives in:

- GitHub issue `#1`
- the issue backlog created from that PRD

If we need a new build order, write it against the current issue graph rather than reviving this document.
