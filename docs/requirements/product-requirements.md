# Archived Product Requirements

This document is obsolete.

It described an older Codex-first `v1` direction that is no longer the active product plan.

Use these instead:

- [CONTEXT.md](/home/jay/dev/Tools/corporate_standardized_agents/CONTEXT.md:1)
- GitHub issue `#1` for the active PRD
- the GitHub issue backlog for execution order and current scope

Do not implement from this file unless it is rewritten to match the PRD.
